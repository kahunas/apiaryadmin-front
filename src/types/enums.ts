export enum Role{
    ADMIN = "Admin",
    USER = "ApiaryUser",
    GUEST = "Guest"
}

export enum Method {
    POST = "POST",
    GET = "GET",
    PUT = "PUT",
    PATCH = "PATCH",
    DELETE = "DELETE",
}